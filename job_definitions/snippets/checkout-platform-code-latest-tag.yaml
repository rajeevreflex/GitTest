---
- job-template:
    name: '{name}-checkout-platform-code-latest-tag'
    project-type: freestyle
    defaults: global
    description: 'Do not edit this job through the web!'
    disabled: false
    display-name: 'Latest tagged platform code Checkout for {name}'
    node: '{name}'
    properties:
      - build-discarder:
          days-to-keep: 365
          num-to-keep: 100
      - delivery-pipeline:
          stage: Build
          task: Checkout Code
    wrappers:
      - ansicolor
    scm:
      - '{config_code_repository_type}':
          refspec: '{config_code_repository_refspec}'
          url: '{config_code_repository_uri}'
          branches:
            - '{config_code_repository_branches}'
          credentials-id: '{config_code_repository_credentials}'
          git-config-name: '{config_code_repository_display_name}'
          git-config-email: '{config_code_repository_display_email}'
          timeout: 5
    publishers:
      - pipeline:
          project: '{name}-build-artefact'
          current-parameters: true
          predefined-parameters: PLATFORM_RELEASE_ID=${{GIT_COMMIT}}
